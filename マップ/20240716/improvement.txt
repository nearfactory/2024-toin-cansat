
＜＜ 改善点 ＞＞

・webSerial→outputに名前変更

・コンソールを開かなくても見えるようにしたよ

・緯度：○○, 経度：○○　の形で出力されるようにしたよ

・緯度 / 経度の最小値、最大値を設定できるようにしたよ、この範囲を超えると自動で出力が停止されるようにしたよ

・INFO、ERRORの体裁を修正したよ

・仮でArduino用模擬GPSデータを出力するプログラムを作ったよ



＜＜ これからやること ＞＞

・状況データを反映させる

・緯度経度が多少の誤差なら出力しないようにする

・マップ画像を出力するようにする

・マップ上に点をプロットする
